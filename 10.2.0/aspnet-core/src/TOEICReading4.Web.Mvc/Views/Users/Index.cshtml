@using TOEICReading4.Web.Startup
@using TOEICReading4.Web.Models.Users
@model UserListViewModel
@{
    ViewBag.Title = L("Users");
    ViewBag.CurrentPageName = PageNames.Users;
}
@section scripts
{
    <environment names="Development">
        <script src="~/view-resources/Views/Users/Index.js" asp-append-version="true"></script>
    </environment>

    <environment names="Staging,Production">
        <script src="~/view-resources/Views/Users/Index.min.js" asp-append-version="true"></script>
    </environment>
}

<div class="app-page">
    <section class="app-hero">
        <div class="app-hero-content">
            <div>
                <div class="app-hero-badges">
                    <span class="app-chip"><i class="fas fa-users"></i>@L("Users")</span>
                </div>
                <h1>@L("Users")</h1>
            </div>

            <div class="app-actions">
                <a href="javascript:;" data-bs-toggle="modal" data-bs-target="#UserCreateModal" class="btn btn-primary">
                    <i class="fa fa-plus-square me-2"></i>@L("Create")
                </a>
            </div>
        </div>
    </section>

    <section class="app-panel">
        <div class="app-panel-header">
            <div>
                <h2>@L("Users")</h2>
            </div>
            @await Html.PartialAsync("~/Views/Users/Index.AdvancedSearch.cshtml")
        </div>

        <div class="app-table-wrap">
            <table id="UsersTable" class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th></th>
                        <th>@L("UserName")</th>
                        <th>@L("FullName")</th>
                        <th>@L("EmailAddress")</th>
                        <th>@L("IsActive")</th>
                        <th style="width: 150px">@L("Actions")</th>
                    </tr>
                </thead>
                <tbody></tbody>
            </table>
        </div>
    </section>
</div>

@await Html.PartialAsync("~/Views/Users/_CreateModal.cshtml", Model)

<div class="modal fade" id="UserEditModal" tabindex="-1" role="dialog" aria-labelledby="UserEditModalLabel" data-bs-backdrop="static">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
        </div>
    </div>
</div>
